function addMember() {
    const member = document.getElementById('member-add').value;
    if (member != "") {
        // add the value to database here
    }
}

function addParam() {
    const param = document.getElementById('member-add').value;
}