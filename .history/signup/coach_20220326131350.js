function addMember() {
    const member = document.getElementById('member-add').value;
    if (member != "") {
        // add the value to database here
    }
}

function addParam() {
    const param = document.getElementById('param-add').value;
    if (param != "") {
        // add the param to datana
    }
}